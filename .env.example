# Figma personal access token, with the file_read scope.
# Figma -> Settings -> Personal access tokens.
# Optional: the UI also accepts a token per-audit if this is not set.
FIGMA_TOKEN=

# Where evidence screenshots are written. Defaults to ./.uixray
# UIXRAY_DATA_DIR=.uixray
